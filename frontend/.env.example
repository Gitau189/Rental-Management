# Base URL for the Django API.
# Development: leave blank — Vite's dev proxy handles /api → http://localhost:8000/api
# Production:  set to your deployed API URL (no trailing slash)
VITE_API_BASE_URL=
