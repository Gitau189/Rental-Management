SECRET_KEY=your-very-secret-key-change-this
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

DB_NAME=rental_db
DB_USER=postgres
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432

# Comma-separated list of frontend origins allowed to call the API
# Development: http://localhost:5173
# Production:  https://yourdomain.com
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional: automatically create a superuser after migrations when these are set
# Example values (do NOT commit real passwords):
SUPERUSER_USERNAME=admin
SUPERUSER_EMAIL=mohadek2004@gmail.com
SUPERUSER_PASSWORD=mohadek123
# Optional role to assign to the created superuser (defaults to 'landlord')
SUPERUSER_ROLE=landlord
